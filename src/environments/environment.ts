export const environment = {
    production: false,
    //environment variables are used to store the configuration settings of the application
    //production is false because we are in the development environment
    apiUrl: 'https:/demodata.grapecity.com/northwind/api/v1/'
    };